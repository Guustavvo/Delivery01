<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/views/css/produtos.css">
    <title>Document</title>
</head>
<body>
    <header>

            <h4>Delivery</h4>

            <div id="voltar">
                <a href="/categorias">Voltar</a>
           </div>

            {{#each produto}}
            <a href="/det_produto/{{id}}">
            <div class="menu">
                <img src="/views/uploads/{{imagem}}" alt="">
                <p class="nomeProduto">{{nome}}</p>
                <p class="descProduto">{{desc}}</p>
                <p class="precoProduto">R${{preco}}</p>
            </div>
            </a>
            {{/each}}
           
    </header>
</body>
</html>